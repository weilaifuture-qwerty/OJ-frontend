// Sentry error tracking
// Temporarily disabled during Vue 3 migration
// TODO: Re-implement with @sentry/vue package for Vue 3 support

export default {
  install() {
    // Placeholder for Sentry integration
    console.log('Sentry integration disabled during migration')
  }
}